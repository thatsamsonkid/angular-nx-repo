# feature-banner

Buildable-only banner feature. Consumed through `@angular-nx-repo/elements`,
not by the Angular app directly.

`BannerElementModule.customElementComponent` is the ngx-element hook.

```sh
npx nx test feature-banner
npx nx build feature-banner
```
