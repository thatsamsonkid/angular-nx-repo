# feature-gallery

Buildable-only gallery feature. Consumed through `@angular-nx-repo/elements`,
not by the Angular app directly.

`GalleryElementModule.customElementComponent` is the ngx-element hook.

```sh
npx nx test feature-gallery
npx nx build feature-gallery
```
