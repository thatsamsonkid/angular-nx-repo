/**
 * Publishable UI kit. Components live on secondary entry points so a host
 * can import one widget without pulling the rest of the package.
 *
 * Example:
 *   import { Button, defineUiButton } from '@angular-nx-repo/ui/button';
 */
export { UI_PACKAGE } from './lib/ui-package';
